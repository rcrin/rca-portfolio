import React from "react";
import "./Header.css";
import Menu from "../Menu/Menu";

const Header = () => {
  return (
    <h1 className="header-menu">
      <Menu />
    </h1>
  );
};

export default Header;
